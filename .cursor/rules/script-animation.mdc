---
description: 逐字稿→plan：动画是指令；已知走绿标；练习先拍照。机械项交给 normalize-plan.mjs
globs: "**/plan.json,tools/normalize-plan.mjs"
alwaysApply: false
---

# 逐字稿 → plan

只靠这一条规则 + `node tools/normalize-plan.mjs <plan.json>`。不要再另写 md。

- **小环节**才是板书。「已知 / 求 / 问」进 `section.tag`，其余进 `lead`（已知 `tagTone: "known"`）。脚本会拆漏写的「已知…」整句。
- **动画列**每个带圈序号一步 action，只执行动词：高亮 → `highlights`（审题打在题干）；出现 → 长出内容；合并为 → 同一 `replaceKey` 换成结果。禁止把「出现 / 高亮 / 合并为 / 得到」写进 `lines`。
- **练习**：进容器先 `练习-作答-拍照`（只出题干），再审题。脚本会把拍照插到练习第一步。

```json
// 错：{ "type": "text", "lines": ["已知万级是28，个级是506。"] }
// 对：
{ "type": "section", "tag": "已知", "tagTone": "known", "lead": "万级是28，个级是506。" }
// 合并：先 28｜0506，下一步同一 replaceKey 换成 280506
```
